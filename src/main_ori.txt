#include <Arduino.h>

/* Custom */
#include <display_tile_driver.h>

/* Image Array */
#include <image_960x240.h>

// Create display driver with CS pins
DisplayTileDriver display(4, 5, 6, 7, 21);

void setup() {
    Serial.begin(115200);
    delay(1000);

  // Initialize all displays
    Serial.println("Initializing display tiles...");
    display.begin();
    Serial.println("Displays initialized!");
    
    // Diagnosis Test Pattern
    // display.displayTestPattern();
    
    // Display Image
    Serial.println("Displaying image...");
    display.displayImagePROGMEM(image, IMG_FULL_WIDTH, IMG_FULL_HEIGHT);
    Serial.println("Done!");
}

void loop() {
    // Nothing here
}